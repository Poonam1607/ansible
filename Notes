Using Password
Go to the file /etc/ssh/sshd_config.d/60-cloudimg-settings.conf
Update PasswordAuthentication yes
sudo vim /etc/ssh/sshd_config - uncomment this - #PasswordAuthentication yes
Restart SSH -> sudo systemctl restart ssh
Set password for it - sudo passwd ubuntu

ssh-copy-id ubuntu@ip-add
